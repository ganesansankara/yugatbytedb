#FROM ubuntu
#USER root
#RUN apt-get update && apt-get upgrade -y
#RUN apt-get install -y unixodbc unixodbc-dev python3 python3-pip

FROM apache/superset:latest
USER root

#USER superset
RUN  apt-get update &&  apt update &&  apt install -y unixodbc unixodbc-dev vim


COPY afterscripts /tmp/afterscripts

RUN /bin/bash -c 'source /tmp/afterscripts/pyenvsettings.sh; echo $PYENV_ROOT $DEVCONTAINREMOTE_DEVCONTAINER_HOMEER_HOME'
RUN /bin/bash -c '(cd /tmp/afterscripts; sh -x supersetinit.sh)'

USER superset
EXPOSE 8080
#ENTRYPOINT ["/bin/bash", ""]

